# 2025年9月22日

## lock、mutex、死锁的使用与理解

### 基本概念理解：

1. 理解std::mutex（互斥锁）是C++11中用于保护共享数据免受多个线程同时访问导致的竞态条件的基本同步机制。
2. 理解std::lock_guard和std::unique_lock是RAII（Resource Acquisition Is Initialization）风格的锁管理类，用于自动管理锁的获取和释放。
3. 理解死锁的概念及其产生的原因，包括两个或多个线程互相等待对方释放资源而无限期地阻塞。

### mutex的使用：

1. 能够正确使用std::mutex来同步访问共享数据。
2. 理解并避免在持有锁的情况下执行长时间的操作，以减少锁的竞争和潜在的死锁风险。

### lock_guard和unique_lock的使用：

1. 能够在函数作用域内使用std::lock_guard自动管理锁的生命周期。
2. 能够使用std::unique_lock进行更灵活的锁管理，包括延迟锁定、条件等待等。

### 避免死锁：

理解避免死锁的策略，如始终按相同的顺序获取锁、使用超时机制等。

1. 理解RAII lock，理解unique_lock和lock_guard的区别？
2. 掌握除了mutex外的其他几种互斥量，比如递归锁、读写锁，并写出实际应用代码。
3. 锁的底层实现原理是怎么样的？