# 2025年9月21日

## [61. 旋转链表](https://leetcode.cn/problems/rotate-list/)

给你一个链表的头节点 `head` ，旋转链表，将链表每个节点向右移动 `k` 个位置。

方法一：快慢双指针

​	先获取链表的节点个数`n`，因为`k`有可能比`n`大，每有一个`n`时就相当于原链表，所以`k`对`n`取余出`new_k`，只要结果不等于0，就说明需要移动，此时让快指针`fast`先移动`new_k`个节点，然后`fast`指针和`slow`指针一起遍历链表，直到`fast`指针到达链表尾部，此时`new_head=slow->next`就是新链表的头部，然后把链表断开重连就好了，最后返回链表。具体代码如下

```C++
class Solution {
public:
    ListNode* rotateRight(ListNode* head, int k) {
        if(!head){
            return head;
        }
        ListNode* fast=head;
        ListNode* slow=head;
        ListNode* temp=head;
        int len=0;
        while(temp){
            ++len;
            temp=temp->next;
        }
        k%=len;
        if(!k){
            return head;
        }

        while(k--){
            fast=fast->next;
        }

        while(fast->next){
            slow=slow->next;
            fast=fast->next;
        }

        temp=slow->next;
        slow->next=nullptr;
        fast->next=head;
        return temp;
    }
};
```

方法二：闭合成环

​	其实和快慢双指针整体思路差不多，只是减少断开重连的操作，也是先获取链表长度`n`，然后取余计算需要断开的位置，然后移动过去，`new_head`就是节点下一个节点，然后断开连接，具体代码如下

```C++
class Solution {
public:
    ListNode* rotateRight(ListNode* head, int k) {
        if (k == 0 || head == nullptr || head->next == nullptr) {
            return head;
        }
        int n = 1;
        ListNode* iter = head;
        //获取链表长度
        while (iter->next != nullptr) {
            iter = iter->next;
            n++;
        }
        //移动k次，也就是移动末尾倒数k个节点到前面，所以用n-k对n取余的实际移动量，就是新链表链表尾的位置
        int add = n - k % n;
        if (add == n) {
            return head;
        }
        //连成环
        iter->next = head;
        //开始移动到断开位置
        while (add--) {
            iter = iter->next;
        }
        //新链表头部就是当前节点的下一个节点
        ListNode* ret = iter->next;
        //断开
        iter->next = nullptr;
        //返回新头指针
        return ret;
    }
};
```

## [142. 环形链表 II](https://leetcode.cn/problems/linked-list-cycle-ii/)

给定一个链表的头节点  `head` ，返回链表开始入环的第一个节点。 *如果链表无环，则返回 `null`。*

如果链表中有某个节点，可以通过连续跟踪 `next` 指针再次到达，则链表中存在环。 为了表示给定链表中的环，评测系统内部使用整数 `pos` 来表示链表尾连接到链表中的位置（**索引从 0 开始**）。如果 `pos` 是 `-1`，则在该链表中没有环。**注意：`pos` 不作为参数进行传递**，仅仅是为了标识链表的实际情况。

**不允许修改** 链表。

方法一：哈希集合，类似相交链表哈希集合一样，依次把节点放入哈希集合中，然后判断节点下一个节点，如果出现在集合中，那就说明有环，如果没有重复，就说明没有环。具体代码如下。

```C++
class Solution {
public:
    ListNode* detectCycle(ListNode* head) {
        unordered_set<ListNode*> un_set;
        while (head != nullptr) {
            if (un_set.find(head) != un_set.end()) {
                return head;
            }
            un_set.insert(head);
            head = head->next;
        }
        return nullptr;
    }
};
```

方法二：快慢双指针遍历，如果链表有环存在，那么一快一慢双指针时一定会相遇的。具体思路我直接复制力扣官方解答吧

思路与算法

我们使用两个指针，fast 与 slow。它们起始都位于链表的头部。随后，slow 指针每次向后移动一个位置，而 fast 指针向后移动两个位置。如果链表中存在环，则 fast 指针最终将再次与 slow 指针在环中相遇。

如下图所示，设链表中环外部分的长度为 a。slow 指针进入环后，又走了 b 的距离与 fast 相遇。此时，fast 指针已经走完了环的 n 圈，因此它走过的总距离为 a+n(b+c)+b=a+(n+1)b+nc。

<img src="https://assets.leetcode-cn.com/solution-static/142/142_fig1.png" alt="fig1" style="zoom:30%;" />

根据题意，任意时刻，fast 指针走过的距离都为 slow 指针的 2 倍。因此，我们有

a+(n+1)b+nc=2(a+b)⟹a=c+(n−1)(b+c)
有了 a=c+(n−1)(b+c) 的等量关系，我们会发现：从相遇点到入环点的距离加上 n−1 圈的环长，恰好等于从链表头部到入环点的距离。

因此，当发现 slow 与 fast 相遇时，我们再额外使用一个指针 ptr。起始，它指向链表头部；随后，它和 slow 每次向后移动一个位置。最终，它们会在入环点相遇。

具体代码：

```C++
class Solution {
public:
    ListNode *detectCycle(ListNode *head) {
        ListNode *slow = head, *fast = head;
        while (fast != nullptr) {
            slow = slow->next;
            if (fast->next == nullptr) {
                return nullptr;
            }
            fast = fast->next->next;
            if (fast == slow) {
                ListNode *ptr = head;
                while (ptr != slow) {
                    ptr = ptr->next;
                    slow = slow->next;
                }
                return ptr;
            }
        }
        return nullptr;
    }
};
```



## chrono的使用与理解

### 基本概念理解：

1. 理解 std::chrono 是 C++11 引入的用于日期和时间的库。
2. 理解 std::chrono 中的时间点（time_point）、持续时间（duration）、时钟（clocks）等基本概念。
3. 能够区分系统时钟（system_clock）、稳定时钟（steady_clock）和高精度时钟（high_resolution_clock）等不同类型的时钟。

### duration和time_point的使用：

1. 能够使用 std::chrono::duration 来表示一段时间，并理解其内部类型（如 std::chrono::seconds、std::chrono::milliseconds 等）。
2. 能够使用 std::chrono::time_point 来表示一个具体的时间点，通常与某个时钟相关联。
3. 能够进行时间点的算术运算（如加法、减法）和持续时间与时间点之间的转换。

### 时钟clock的使用：

1. 能够获取当前时间点的不同表示（如系统时间、稳定时间）。
2. 理解不同时钟之间的精度和用途差异。

### time_point的输出：

1. 能够将时间点转换为可读的字符串形式（std::local_time）。

掌握chrono中duration的实现原理。

## 多线程、并发编程

### 基本概念理解：

1. 理解C++11引入的线程库（<thread>），包括线程的基本概念、创建、同步和销毁。
2. 理解线程安全、竞态条件、死锁等并发编程中的常见问题及解决方案。
3. 了解互斥锁（std::mutex）、条件变量（std::condition_variable）、锁保护（std::lock_guard、std::unique_lock）等同步机制。

### 线程的创建与管理：

1. 能够使用std::thread类创建和管理线程。
2. 理解如何传递参数给线程函数，并正确接收线程的返回值（如果有的话）。
3. 知道如何等待线程结束（join()）或分离线程（detach()）。

### 同步机制的使用：

1. 能够使用std::mutex保护共享数据，防止竞态条件。
2. 理解并使用std::lock_guard和std::unique_lock管理锁的生命周期，以减少死锁的风险。
3. 能够使用std::condition_variable实现线程间的同步等待/通知机制。

### 高级话题：

1. 了解并理解线程局部存储（TLS, thread_local）的概念和使用场景。
2. 了解并尝试使用线程池等高级并发工具（项目中会使用）。

1. future、promise、thread、packaged_task、async几者之间有什么关系？
2. 理解async的几个启动模式，会不会有什么坑呢？