# 2025年9月27日

## 进程间通信

### 基本概念理解：

a. 理解进程间通信（IPC）的定义及其在操作系统中的作用。

b. 掌握C++中常见的IPC机制，包括管道（Pipes）、消息队列（Message Queues）、共享内存（Shared Memory）、信号量（Semaphores）和套接字（Sockets）。

c. 理解每种IPC机制的工作原理、优缺点以及适用场景。

### 实现细节：

a. 能够使用C++标准库或系统调用（如POSIX API）创建和管理管道、消息队列、共享内存和信号量。

b. 掌握如何在C++中使用套接字进行网络通信，包括TCP和UDP协议的基本使用。

### 性能考量：

a. 理解不同IPC机制在性能上的差异，包括数据传输速度、资源消耗等。

b. 能够根据具体应用场景选择合适的IPC机制，并优化通信性能。

1. Windows中一定要掌握命名管道通信。
2. Linux中一定要掌握共享内存通信
3. 可以研究如何使用zmq进行进程间通信。

## [705. 设计哈希集合](https://leetcode.cn/problems/design-hashset/)

不使用任何内建的哈希表库设计一个哈希集合（HashSet）。

实现 `MyHashSet` 类：

- `void add(key)` 向哈希集合中插入值 `key` 。
- `bool contains(key)` 返回哈希集合中是否存在这个值 `key` 。
- `void remove(key)` 将给定值 `key` 从哈希集合中删除。如果哈希集合中没有这个值，什么也不做。

```C++
class MyHashSet {
private:
    std::vector<list<int>> data;
    static const int hash_value = 123;
    static int hash(int key) { return key % hash_value; }

public:
    MyHashSet() : data(hash_value) {}

    void add(int key) {
        int value = hash(key);
        for (auto it = data[value].begin(); it != data[value].end(); it++) {
            if ((*it) == key) {
                return;
            }
        }
        data[value].push_back(key);
    }

    void remove(int key) {
        int value = hash(key);
        for (auto it = data[value].begin(); it != data[value].end(); it++) {
            if ((*it) == key) {
                data[value].erase(it);
                return;
            }
        }
    }

    bool contains(int key) {
        int value = hash(key);
        for (auto it = data[value].begin(); it != data[value].end(); it++) {
            if ((*it) == key) {
                return true;
            }
        }
        return false;
    }
};

/**
 * Your MyHashSet object will be instantiated and called as such:
 * MyHashSet* obj = new MyHashSet();
 * obj->add(key);
 * obj->remove(key);
 * bool param_3 = obj->contains(key);
 */
```

## [706. 设计哈希映射](https://leetcode.cn/problems/design-hashmap/)

不使用任何内建的哈希表库设计一个哈希映射（HashMap）。

实现 `MyHashMap` 类：

- `MyHashMap()` 用空映射初始化对象
- `void put(int key, int value)` 向 HashMap 插入一个键值对 `(key, value)` 。如果 `key` 已经存在于映射中，则更新其对应的值 `value` 。
- `int get(int key)` 返回特定的 `key` 所映射的 `value` ；如果映射中不包含 `key` 的映射，返回 `-1` 。
- `void remove(key)` 如果映射中存在 `key` 的映射，则移除 `key` 和它所对应的 `value` 。

没什么大变化，主要是存储的数据结构有一点变化，从值变成值对了。

```C++
class MyHashMap {
private:
    std::vector<list<pair<int, int>>> data;
    static cosnt int hash_value = 123;
    static int hash(int key) { return key % hash_value; }

public:
    MyHashMap() : data(hash_value) {}

    void put(int key, int value) {
        int hvalue = hash(key);
        for (auto it = data[hvalue].begin(); it != data[hvalue].end(); it++) {
            if ((*it).first == key) {
                (*it).second = value;
                return;
            }
        }
        data[hvalue].push_back(make_pair(key, value));
    }

    int get(int key) {
        int hvalue = hash(key);
        for (auto it = data[hvalue].begin(); it != data[hvalue].end(); it++) {
            if ((*it).first == key) {
                return (*it).second;
            }
        }
        return -1;
    }

    void remove(int key) {
        int hvalue = hash(key);
        for (auto it = data[hvalue].begin(); it != data[hvalue].end(); it++) {
            if ((*it).first == key) {
                data[hvalue].erase(it);
                return;
            }
        }
    }
};

/**
 * Your MyHashMap object will be instantiated and called as such:
 * MyHashMap* obj = new MyHashMap();
 * obj->put(key,value);
 * int param_2 = obj->get(key);
 * obj->remove(key);
 */
```

