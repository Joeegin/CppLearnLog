# 2025年9月24日

## 理解函数调用的原理

### 基本概念理解：

1. 理解函数调用是程序执行时跳转到函数定义处执行代码，并在执行完毕后返回原调用点继续执行的过程。
2. 理解函数调用过程中涉及的参数传递（值传递、指针传递、引用传递）及其区别。
3. 理解函数调用的栈帧（Stack Frame）概念，包括局部变量、参数、返回地址等的存储方式。

### 函数调用的实现：

1. 能够编写包含函数调用和参数传递的代码。
2. 理解并解释函数调用的开销，包括参数压栈、返回地址保存等。

### 内联函数与递归函数：

1. 理解内联函数的作用及其实现机制，包括编译器对内联函数的优化。
2. 能够编写递归函数，并理解递归调用的基本思想和终止条件的重要性。

### 函数指针与回调函数：

1. 理解函数指针的概念，能够定义和使用函数指针。
2. 理解回调函数的概念，能够在程序中实现和使用回调函数。

理解栈帧的概念？

为函数调用在计算机函数调用栈区分配的内存块，一个函数被调用时，就会push一个栈帧，调用完毕返回时，就会pop一个栈帧。

[LCR 183. 望远镜中最高的海拔](https://leetcode.cn/problems/hua-dong-chuang-kou-de-zui-da-zhi-lcof/)

科技馆内有一台虚拟观景望远镜，它可以用来观测特定纬度地区的地形情况。该纬度的海拔数据记于数组 `heights` ，其中 `heights[i]` 表示对应位置的海拔高度。请找出并返回望远镜视野范围 `limit` 内，可以观测到的最高海拔值。

方法一：优先队列，因为需要最大值，所以可以利用优先队列的`top`始终为堆中最大值这个特性实现自动找出滑动窗口内`limit`个数的最大值。然后每次往后移动的时候，`pop`出超过滑动窗口范围的索引，也就是用当前索引`i`减去`limit`大小，就是`limit`窗口前的最后索引，此时超过了窗口大小，所以其实是没必要留在优先队列中，可以直接`pop`出去。然后再保存优先队列的`top`就是新窗口内的最大值。最后直到结束。具体实现看如下代码：

```C++
class Solution {
public:
    vector<int> maxAltitude(vector<int>& heights, int limit) {
        int n = heights.size();
        //首先检查数组
        if (n == 0) {
            return {};
        }
        //创建一个保存val和索引对值的优先队列
        priority_queue<pair<int, int>> q;
        //先将limit个元素存入进来
        for (int i = 0; i < limit; ++i) {
            q.emplace(heights[i], i);
        }
        //此时，优先队列top元素的first就是最大值，将它保存
        vector<int> ans = {q.top().first};
        //滑动窗口开始向后移动，
        for (int i = limit; i < n; ++i) {
            //移动一次后，先将这个对插入进队列中，
            q.emplace(heights[i], i);
            //将超过窗口大小索引的值对移除
            while (q.top().second <= i - limit) {
                q.pop();
            }
            //移除完窗口前的节点后，将此时优先队列的top保存，此时优先队列中的top就是新窗口的最大值
            ans.push_back(q.top().first);
        }
        //最后返回这个vector
        return ans;
    }
};
```

# 2025年9月25日

## 多态原理的理解

### 底层原理的理解：

1. 理解多态的原理

   静态多态：通过模板，重载函数实现编译期多态。

   动态多态：子类继承基类，重写基类虚函数，通过虚函数表指针指向虚函数表，通过虚函数表中记录的虚函数，在运行时检查调用。

2. 理解面向对象的布局原则

   有虚函数的情况下，对象内存开始是一个虚函数表指针，然后是类内成员变量（按声明顺序）。

3. 理解虚函数表

1. 多态的原理如何？为什么可以实现多态？

2. 无继承场景、单继承场景、多继承场景下，对象布局如何？

   多继承时，按继承顺序分别是【继承A的虚函数表指针，继承A的成员变量，继承B的虚函数表指针，继承B的成员变量......,最后时本身的成员变量】

3. 构造函数可以为虚函数吗？

   不可以，对象创建时，依赖构造函数，虚函数表指针依赖对象内存，此时对象还未创建；虚函数机制需要对象存在，但是构造函数是用来创建对象的。

4. 析构函数可以为虚函数吗？

   可以，而且最好是虚函数，在通过基类指针删除子类对象时，可以正确的析构子类对象；避免资源泄漏。

5. 理解final关键字的作用

   1）禁止类被继承；2）禁止虚函数被重写。

6. 理解override关键字的作用

   明确表明这是在重写虚函数。可以让编译期进行一些检查。

## 哈希表相关内容

1. 能准确描述哈希表的定义，能够通过哈希函数将键映射到存储位置的数据结构

2. 理解哈希冲突的概念及产生原因，了解常见的哈希函数及常见的冲突解决（拉链法、开放寻址法）的方法

[146. LRU 缓存](https://leetcode.cn/problems/lru-cache/)

请你设计并实现一个满足 [LRU (最近最少使用) 缓存](https://baike.baidu.com/item/LRU) 约束的数据结构。

实现 `LRUCache` 类：

- `LRUCache(int capacity)` 以 **正整数** 作为容量 `capacity` 初始化 LRU 缓存
- `int get(int key)` 如果关键字 `key` 存在于缓存中，则返回关键字的值，否则返回 `-1` 。
- `void put(int key, int value)` 如果关键字 `key` 已经存在，则变更其数据值 `value` ；如果不存在，则向缓存中插入该组 `key-value` 。如果插入操作导致关键字数量超过 `capacity` ，则应该 **逐出** 最久未使用的关键字。

函数 `get` 和 `put` 必须以 `O(1)` 的平均时间复杂度运行。

代码解答：

​	具体解析可参考官方讲解，官方讲解已经足够清晰详略、简单易懂

```C++
class LRUCache {
private:
    struct LinkNode {
        int key, value;
        LinkNode* prev;
        LinkNode* next;
        LinkNode() : key(0), value(0), prev(nullptr), next(nullptr) {}
        LinkNode(int pkey, int pvalue)
            : key(pkey), value(pvalue), prev(nullptr), next(nullptr) {}
    };
    std::unordered_map<int, LinkNode*> map;
    LinkNode* head;
    LinkNode* tail;
    int size, capacity;

public:
    LRUCache(int pcapacity) : capacity(pcapacity), size(0) {
        head = new LinkNode();
        tail = new LinkNode();
        head->next = tail;
        tail->prev = head;
    }

    int get(int pkey) {

        if (map.find(pkey) == map.end()) {
            return -1;
        }

        LinkNode* node = map[pkey];
        moveTohead(node);
        return node->value;
    }

    void put(int pkey, int pvalue) {
        if (map.find(pkey) == map.end()) {
            LinkNode* node = new LinkNode(pkey, pvalue);

            map[pkey] = node;
            addToHead(node);
            ++size;
            if (size > capacity) {
                LinkNode* removed = removeTail();
                map.erase(removed->key);
                delete removed;
                --size;
            }
        } else {
            LinkNode* node = map[pkey];
            node->value = pvalue;
            moveTohead(node);
        }
    }

    void addToHead(LinkNode* node) {
        node->prev = head;
        node->next = head->next;
        head->next->prev = node;
        head->next = node;
    }

    void removeNode(LinkNode* node) {
        node->prev->next = node->next;
        node->next->prev = node->prev;
    }

    void moveTohead(LinkNode* node) {
        removeNode(node);
        addToHead(node);
    }

    LinkNode* removeTail() {
        LinkNode* node = tail->prev;
        removeNode(node);
        return node;
    }
};

/**
 * Your LRUCache object will be instantiated and called as such:
 * LRUCache* obj = new LRUCache(capacity);
 * int param_1 = obj->get(key);
 * obj->put(key,value);
 */