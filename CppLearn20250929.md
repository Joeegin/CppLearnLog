# 2025年9月29日

## filesystem

1. 基本概念

a. 理解std::filesystem的基本概念和常用术语，路径表示（绝对路径、相对路径、路径分隔符）、文件类型（普通文件、目录、符号链接）、文件状态（文件大小、创建时间、最后修改时间、文件权限）等

2. 掌握std::filesystem::path类的使用，能够进行路径的拼接、分解、遍历等操作

a. std::filesystem::path 类的构造和成员函数。

b. 路径拼接：operator/, append(), concat()。

c. 路径分解：parent_path(), filename(), extension()。

d. 路径遍历：begin(), end(), iterator。

e. 路径比较：==, !=, <, > 等运算符。

3. 文件和目录操作，能够进行文件的创建、删除、复制、移动等操作

a. 文件操作：

(i)创建文件：create_directory(), create_directories()。

(ⅱ)删除文件：remove(), remove_all()。

(ⅲ)复制文件：copy(), copy_file()。

(ⅳ)移动文件：rename()。

(ⅴ)文件状态查询：exists(), is_regular_file(), is_directory() 等。

b. 目录操作：

(i)创建目录：create_directory(), create_directories()。

(ⅱ)删除目录：remove(), remove_all()。

(ⅲ)遍历目录：directory_iterator, recursive_directory_iterator。

(ⅳ)目录状态查询：exists(), is_directory() 等。

1. 如何判断一个文件是否存在？
2. 尝试删除一个不存在的文件，并捕获和处理异常。