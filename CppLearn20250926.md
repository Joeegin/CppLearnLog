# 2025年9月26日

## 内存对齐

### 基本概念理解：

a. 理解内存对齐的概念及其在计算机体系结构中的重要性。

b. 理解不同数据类型（如int, float, double, struct等）的内存对齐规则。

c. 理解为什么要进行堆存对齐。

### 对齐规则与实践：

a. 掌握编译器如何进行内存对齐，以及如何通过编译器指令（如#pragma pack）来修改对齐规则。

b. 能够计算结构体（struct）的内存布局，包括填充字节（Padding）的计算。

c. 理解对齐对性能的影响，特别是在缓存一致性和内存访问效率方面的作用。

### 对齐与性能优化：

a. 理解内存对齐如何影响缓存利用率和内存访问速度。

b. 能够分析和优化结构体（struct）的内存布局，以减少填充字节，提高内存访问效率。



能够理解内存对齐规则，在实际设计结构体时，保证占用空间最小。



## 返回值优化

### 基本概念理解：

a. 理解C++ RVO（返回值优化）的概念及其在现代C++编译器中的实现。

b. 理解RVO如何通过消除不必要的临时对象复制来提高性能。

c. 理解NRVO（命名返回值优化）作为RVO的一种特殊情况，并知道它是如何工作的。

### 编译器行为分析：

a. 能够识别编译器是否对特定函数进行了RVO优化。

b. 理解编译器如何决定对哪些函数进行RVO优化，以及哪些情况下可能无法进行优化。

### 代码编写与优化：

a. 能够编写可能受益于RVO优化的函数，并理解如何通过代码结构促进RVO。

b. 理解在哪些情况下RVO可能不适用，并知道如何手动优化以避免性能瓶颈。

### 高级话题：

a. 理解移动语义（Move Semantics）与RVO之间的关系，以及它们如何共同作用于性能优化。

b. 了解C++11及更高版本中引入的其他优化技术（如Copy Elision），以及它们如何与RVO相互作用。

[无重复字符的最长子串](https://leetcode.cn/problems/longest-substring-without-repeating-characters/)

给定一个字符串 `s` ，请你找出其中不含有重复字符的 **最长 子串** 的长度。

官方讲解比较详细也很容易理解，这里不做过多描述。具体看代码：

```C++
class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        std::unordered_map<char, int> map; //保存字符-索引
        int n = s.size();
        int ans = 0;
        // 滑动窗口左边界
        int left = 0; 
        //通过窗口右边界来向后移动
        for (int right = 0; right < n; ++right) {
            char currentChar = s[right];
            // 如果字符已存在且在当前窗口内，移动左边界，缩小窗口范围，直到窗口内没有重复字符再重新开始扩展右边界
            if (map.find(currentChar) != map.end() && map[currentChar] >= left) {
                left = map[currentChar] + 1;
            }
            
            // 更新字符位置
            map[currentChar] = right;
            
            // 更新最大长度
            ans = max(ans, right - left + 1);
        }
        
        return ans;
    }
};
```

